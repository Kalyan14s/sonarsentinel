# ML dependencies, installed in Sprint 3 (ST-050+).
# Install PyTorch FIRST using the command from https://pytorch.org/get-started/locally/
# for your OS and CUDA version, then:
#   pip install -r backend/requirements-ml.txt
# Pin exact working versions in requirements-ml.lock.txt once validated (anomalib is
# sensitive to torch/lightning versions).
ultralytics>=8.3
sahi>=0.11
anomalib>=1.1
lightgbm>=4.3
scikit-learn>=1.4
onnxruntime>=1.17
