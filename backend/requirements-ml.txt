# ML dependencies (Sprint 3+). Install PyTorch FIRST:
#   CPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA: use the command from https://pytorch.org/get-started/locally/
# then:
#   pip install -r backend/requirements-ml.txt
# Verified on Windows 11 CPU (2026-09-13): torch 2.14.0+cpu, torchvision 0.29.0+cpu,
# ultralytics 8.4.150, sahi 0.12.6, lightgbm 4.7.0, scikit-learn 1.9.1, onnxruntime 1.30.0.
# PatchCore is implemented in sonarsentinel/detect/anomaly.py, so anomalib is not needed (ADR-016).
ultralytics>=8.4,<8.5
sahi>=0.12,<0.13
lightgbm>=4.3
scikit-learn>=1.4
onnxruntime>=1.17
onnx>=1.17          # ONNX export (ml/export_onnx.py, ST-100); verified 1.22.0
onnxslim>=0.1.82    # Ultralytics simplifies exported graphs with it; verified 0.1.96
